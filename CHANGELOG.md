# Changelog

## v1.0.0 — 2026-09-25

### Breaking change：默认目标改为 B22

- 默认支持 `MU5252_HW1.0 / BD_ENCNMU5252V1.0.0B22`；当前时间和触屏安装器会拒绝 B20。B20 源码及验证记录保留在 Git 历史，不应把本版本这些组件直接安装到 B20。
- 本版本是工具包源码发布，不包含或自动安装 B22 固件。记录 B20 → B22 原厂本地 OTA、跨升级保留 root、服务恢复和重启验收，提供原创迁移准备器；升级流程及边界见 [升级记录](docs/OTA-B20-TO-B22.md)。

### 时间与触屏

- 适配 B22 NTP/NITZ 的固件哈希、写钟地址和启动挂载，修复原厂重复叠加时区导致系统 UTC 快 8 小时的问题。
- 根据真实成功的 NITZ/SNTP 写钟事件保存可信偏移，拒绝陈旧、重复或被后续改钟覆盖的事件；保留原厂 base 1 优先、base 12 回退的启动恢复顺序。
- 更新 B22 触屏资源地址和安装检查；修复新版 zwrt-datad 调整 JSON 字段顺序后网络仪表与设备状态无法读取数据的问题。
- 按 IPv4/IPv6 数组读取地址及掩码，修复三网在线时误报“地址或 DNS 缺失”；增加字段重排、嵌套同名字段及采样到诊断的回归测试。

### Mihomo 与 WebUI

- 官方版本查询优先使用运行中的本机代理，失败后回退直连，并限制请求耗时。
- 回收持有进程已退出的操作锁，锁冲突立即返回；版本检查超时后恢复重试按钮，避免页面持续卡在检查状态。
- 核心更新请求短暂断连后核实目标版本和服务恢复，避免重启时误报失败；恢复等待有界，真实失败及权限错误仍明确报告。
- 缩短更新提示，保留页面布局；修复 Mihomo 菜单入口的样式与重复项。
- 恢复重启后的分层 WebUI 挂载和 RPC 服务，保留 Manager 与完整菜单的挂载顺序。

### 设备服务与网络

- 新增可重复执行的 B22 `vendor-control` 脚本：停止并禁用原厂 MQTT，将三路二次认证开关设为 0，并经原厂通知清理认证拦截；保留原值备份和只读检查。它需要显式执行，不停用 ICG 聚合。
- 补齐停用 `zte_dm` 时的启动同步列表调整，修复等待不存在的服务导致的初始化阻塞。
- 明确 mwan3 调优边界：B20/B22 出厂 HTTPS 顺序本来正确，该部分用于 Mihomo/普通 LAN 定制分流；质量检测及按故障线路清理连接在 B22 仍有必要。

### 验证与边界

- 本地 `make check` 通过：ShellCheck、Node 回归、启动钩子、22 项 Timekeeper 测试、时钟事件测试、触屏 JSON/诊断契约及严格 C 编译。macOS 缺少 `flock` 时跳过的锁生命周期测试由 Linux CI 执行。
- B22 实机完成 OTA/root 迁移、时间/触屏适配、服务恢复及重启验证；MQTT/认证关闭经过重复执行、重启与三路 HTTPS 联网验证。
- 尚未在第二台干净设备完成完整安装—重启—卸载，也未在 B22 重跑全部离线/NITZ、单 WAN 故障和模式切换场景。主版本号表示兼容性边界变化，不扩大已验证范围。
- 沿用源码发布，不附预编译 helper、厂商固件/WebUI/字体、设备抓取、凭据或订阅。

## v0.1.2 — 2026-09-14

- 修复 B20 离线开机时原厂 UTC+8 设置与系统显示不一致，补齐 libc 使用的持久化时区文件；
- Timekeeper 改为低频常驻，支持离线超过 10 分钟后联网刷新可信偏移，并增加时区、晚联网及恢复回归测试；
- 修正触屏上传/下载箭头方向；“未知运营商”和大陆四家运营商固定显示，不参与循环滚动；
- 触屏网络仪表将大陆四家运营商归一化显示为“中国移动/中国联通/中国电信/中国广电”；
- 运营商、详情摘要、地址和身份等单行字段仅在溢出时循环滚动，且不再被每秒状态刷新重置动画。

## v0.1.1 — 2026-08-30

- 不再把 `/proc/mounts` 的 source 字段当作单文件 bind mount 的原始文件路径；
- 通过挂载目标存在性与 source/target 的 device:inode 判断挂载所有权；
- start 保持幂等，stop/uninstall 会清理本组件意外叠加的全部顶层挂载，但不触碰无关挂载；
- Mihomo Manager 同步采用所有权检查，并拒绝在完整菜单仍叠加时安装或卸载；
- 增加 B20 实机临时双层 bind mount 回归脚本。

## v0.1.0 — 2026-08-30

TopFlow Toolkit 首个正式版本。

### 设备功能

- 独立 network namespace 中的 Mihomo IPv4 透明网关；
- 接入厂商 WebUI 的 Mihomo 管理页；
- 原生 LVGL 触屏控制中心与三路蜂窝网络仪表；
- Qualcomm `time_genoff` 可信时间持久化；
- 普通 MULTIWAN 的规则、链路质量和选择性 conntrack 调优；
- 从设备当前 WebUI 生成完整隐藏菜单的本地补丁器；
- `zwrt-datad` 手动检查、更新、健康验证与回滚工具。

### 工程边界

- 固件锁定到已验证的 MU5252 B20；
- 不分发厂商二进制、完整 WebUI、字体、图片或修改后的厂商脚本；
- 不保存设备抓取、凭据、订阅、真实代理配置或身份信息；
- 提供分组件卸载、统一恢复、Root ADB、网络架构与 ICG 聚合说明。

公开安装器仍应按实验性改机处理；其中完整 WebUI 菜单和 MULTIWAN wrapper 的新打包
方式已经静态/合成测试，但尚未在第二台干净设备完成安装—重启—卸载全链路验证。
